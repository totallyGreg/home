#!/usr/bin/perl
#
# Code by Kino

use strict;
use utf8;
binmode (STDIN, ":utf8");
binmode (STDOUT, ":utf8");

my $withlf = "NO";

while (<>) {
	if ( s/\n// ) {
		$withlf = "YES";
	}
	print scalar reverse();
	if ( "$withlf" eq "YES" ) {
		print "\n";
		$withlf = "NO";
	}
}

# end of script

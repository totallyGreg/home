#!/bin/bash
## one thought awk 'BEGIN {srand()} !/^$/ { if (rand() <= .01) print $0}'


read through text file into an array
for number of lines needed
randomly generate an array of lines
print random lines from text file
